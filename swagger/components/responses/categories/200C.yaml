description: List of categories
content:
  application/json:
    schema:
      type: object
      properties:
        status:
          type: integer
          example: 200
        message:
          type: string
          example: List of categories for income
        data:
          $ref: ../../schemas/categoriesInformation.yaml
